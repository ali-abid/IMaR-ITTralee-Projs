#!/usr/bin/env node
#! /bin/sh
# /etc/init.d/sayIPbs
## Some things that run always
# Carry out specific functions when asked to by the system

    echo "Button Pressed"
    espeak -v en "Button Pressed"
 
    
